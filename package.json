{
  "name": "express-stormpath-config",
  "version": "0.0.2",
  "description": "The configuration parser / validator for express-stormpath.",
  "main": "index.js",
  "scripts": {
    "test": "istanbul cover node_modules/mocha/bin/_mocha -- --check-leaks test",
    "test-travis": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --reporter spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stormpath/express-stormpath-config.git"
  },
  "keywords": [
    "express",
    "stormpath",
    "authentication",
    "security",
    "configuration"
  ],
  "author": "Stormpath, Inc.",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/stormpath/express-stormpath-config/issues"
  },
  "homepage": "https://github.com/stormpath/express-stormpath-config#readme",
  "devDependencies": {
    "async": "^1.4.2",
    "coveralls": "^2.11.4",
    "get-function-name": "0.0.1",
    "istanbul": "^0.4.4",
    "mocha": "^2.3.2",
    "mocha-lcov-reporter": "1.0.0"
  },
  "dependencies": {
    "deep-extend": "^0.4.0"
  }
}
